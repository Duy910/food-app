const initState = {
    user: null,
}
const actionType = {
    SET_USER: 'SET_USER'
}

const reducer = (state, action) => {
    switch(action.type) {

    }
}
export { initState, actionType }
export default reducer